package SystemTesting;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.support.ui.Select;
import org.testng.Assert;

public class Winiju_Camp_Lead_ST {

	public static void main(String[] args) {
		/*
		 * WebDriver driver = new ChromeDriver();
		 * driver.get("http://192.168.84.128:8080/crm/HomePage.do"); String
		 * expectlogintitle="Zoho CRM - Sign in"; String
		 * actuallogintitle=driver.getTitle(); Assert.assertEquals(actuallogintitle,
		 * expectlogintitle);
		 * driver.findElement(By.id("userName")).sendKeys("rashmi@dell.com");
		 * driver.findElement(By.id("passWord")).sendKeys("123456");
		 * driver.findElement(By.xpath("//input[@title='Sign In']")).click(); String
		 * expecthometitle="Zoho CRM - Home Page"; String
		 * actualhometitle=driver.getTitle(); Assert.assertEquals(actualhometitle,
		 * expecthometitle);
		 * driver.findElement(By.xpath("//a[text()='Campaigns']")).click();
		 * driver.findElement(By.xpath("//input[@value='New Campaign']")).click();
		 * WebElement alllisttype = driver.findElement(By.name("property(Type)"));
		 * Select s=new Select(alllisttype); s.selectByValue("Webinar"); WebElement
		 * allliststatus = driver.findElement(By.name("property(Status)")); Select
		 * s1=new Select(allliststatus); s1.selectByValue("Planning");
		 * driver.findElement(By.xpath("(//img[@title='Calendar'])[2]")).click();
		 * driver.getWindowHandles(). //driver.switchTo().window(actualhometitle);
		 * //driver.findElement(By.xpath("//a[text()='22']")).click();
		 */
	}

}
